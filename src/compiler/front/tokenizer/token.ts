class Token {
    source
    index
    size
    type

    constructor(source, index, size, type) {
        this.source = source
        this.index = index
        this.size = size
        this.type = type
    }
}